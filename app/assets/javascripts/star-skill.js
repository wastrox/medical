$(document).ready(function(){

/* $('.star-pc').raty({
			 target: '.hint-pc',
       width: '136',
       targetKeep: true,
       cancel: false,
       cancelPlace: 'left',
       hints: ['минимальный', 'базовый', 'средний', 'продвинутый', 'свободно']
		});
		    

		$(".star img").click(function(){
      var skill = $(".star input").attr("value");
      var input_skill_language = $(".language_skill").attr("value", skill);
    });
		
		$(".star-pc img").click(function(){
			var skill = $(".star-pc input").attr("value");
			var input_skill_pc = $(".pc-skill").attr("value", skill);
		});
  });

*/

});
