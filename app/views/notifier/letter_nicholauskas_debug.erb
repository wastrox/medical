<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>V! Nicholauskas</h1>
		<ul>
      <% @objects.each do |object| %>
        <li>Вакансия <%= object.name %>--(<%= object.id%>) | <%= object.human_state_name %> | Компания <%= object.company.name %> => <% @time.each do |t| %> 
          <%= t[:day] if t[:id] == object.id %>
          <% end %>
        </li>
      <% end %>
    </ul>
    <p><%#= debug(@msg_error) unless @msg_error.empty? %></p>
    <p>Have a great day!</p>
  </body>
</html>

