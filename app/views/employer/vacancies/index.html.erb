<div class="cap">
  <h2 id="about">Список вакансий</h2>
</div>
<div class="boxed">
<table class="table list-vacancies">
  <tr>
    <th>Название</th>
    <th>Регион</th>
    <th>Статус</th>
    <th></th>
    <!--<th></th>
	  <th></th>-->
  </tr>

<% @vacancies.each do |vacancy| %>
  <tr>
    <td><%= link_to vacancy.name, employer_vacancy_path(vacancy.id) %></td>
	  <td><%= vacancy.city %></td>
    <td><%= vacancy.human_state_name %></td>
    <td><%= link_to 'Редактировать', edit_employer_vacancy_path(vacancy.id), :class => "yellow-link" %></td>
    <!--<td><%# link_to 'Редактировать', edit_employer_vacancy_path(vacancy) %></td>
    <td><%# link_to 'Удалить', ([:employer, vacancy]), method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
  </tr>
<% end %>
  <tr>
    <td colspan="4"></td>
  </tr>
  <tr>
  <td colspan="4"><%= link_to 'Добавить вакансию', new_employer_vacancy_path, :class => "add_fields_for input-sub btn btn-warning" %></td>
  </tr>
</table>

<br />
  <div id="button-bottom">
    <%# link_to 'Добавить вакансию', new_employer_vacancy_path, :class => "add_fields_for input-sub btn btn-warning" %>
  </div>
</div>