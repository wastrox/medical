<div id="frame">
      <div class="cap">
            <% if current_user %>
                  <% if @vacancy.applicants.where(:id => current_user.id).empty? %>
                        <h2 id="about"><%= "#{@vacancy.name} (#{@vacancy.salary} грн.)" %></h2>
                  <% else %>
                        <h2 id="about">Вы откликнулись на эту вакансию,<%= link_to "Мои вакансии", applicant_my_vacancies_index_url %></h2>
                  <% end %>
            <% else %>
                  <h2 id="about"><%= "#{@vacancy.name} (#{@vacancy.experiences}, #{@vacancy.salary} грн.)" %></h2>
            <% end %>
      </div>

      <div class="boxed">
            <% if flash[:notice] %>
                  <br>
                  <div class="alert alert-block alert-success">
                        <p id="notice" ><%= flash[:notice] %></p>
                  </div>
            <% end %>

      	<table cellpadding="0" cellspacing="0" class="photo-name-employer">
      		<tr>
      			<td class="logo-profile"><%= image_tag @vacancy.company.logo.url(:small), :class => "photo-employer" %></td>
      			<td><h2>&#171;<%= link_to @vacancy.company.name, :controller => "search", :action => "company", :id => @vacancy.company.id %>&#187;</h2></td>
      		</tr>
      	</table>
          <table class="paper company" border="0" cellpadding="10">
                              <tr>
                              	<td>Должность</td>
      	                        <td><%= @vacancy.name %></td>
                              </tr>
      						<tr>
      							<td>Зарплата</td>
      							<td><%= @vacancy.salary %> грн.</td>
      						</tr>
                              <tr>
                              	<td>Регион</td>
                              	<td><%= @vacancy.city %></td>
                              </tr>
                              <tr>
                              	<td>Опыт работы</td>
                              	<td><%= @vacancy.experiences %></td>
                              </tr>
                              <tr>
                              	<td>График работы</td>
                              	<td><%= @vacancy.timetable %></td>
                              </tr>
                              <tr>
                              	<td>Описание вакансии</td>
                              	<td><%= @vacancy.description %></td>
                              </tr>
                              <tr>
                              	<th colspan="2" class="line-bottom"></th>
                              </tr>
                              <tr>
                                    <% if current_user %>
                                    	<td>Контактная информация</td>
                                    	<td> <% @vacancy.company.company_contacts.each do |contact| %>
                                    			<div><%= "#{contact.name}: #{contact.phone}" %></v>
                                    		 <% end %>
                                    	</td>
                                    <% else %>
                                          <td colspan="2">Чтобы откликнуться на эту вакансию <%= link_to "Войдите", login_url %> или <%= link_to "Зарегистрируйтесь", signup_url %> и создайте резюме.</td>
                                    <% end %>
                              </tr>
                              <tr>
                                    <th colspan="2" class="line-bottom"></th>
                              </tr>
      				</table>    
                              <% if current_user && current_user.applicant? && current_user.resume != nil %>
                                    <% if @vacancy.applicants.where(:id => current_user.id).empty? %>
                                    <div id="button-bottom">
                                          <%= link_to "Откликнуться на вакансию", applicant_resumes_add_vacancy_responded_path(:vacancy_id => @vacancy.id, :vacancy_name => @vacancy.name), :class => "input-sub btn btn-warning", :id => "btn-yellow-for-link" %>
                                          <div class="clear"></div>
                                    </div>
                                    <% end %>
                              <% end %>
      </div> <!-- #boxed -->
</div> <!-- #frame -->
            <div id="account-contacts">
                  <div class="cap">
                        <h3>Свяжитесь с нами</h3>
                  </div>
                  <div class="boxed-nav">
                        <ul>
                              <li>medical@netbee.ua</li>
                              <span class="line-bottom"></span>
                              <li>(048) 784-14-75</li>
                              <span class="line-bottom"></span>
                              <li>(095) 286-42-32</li>
                              <span class="line-bottom"></span>
                              <li>(095) 286-42-33</li>
                        </ul>
                  </div>
            </div>
<div class="clear"></div>
