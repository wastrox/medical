<ul class="nav nav-tabs nav-stacked"> 
		<li ><a style="background: none repeat scroll 0 0 #EDEDE6;" href="#"><%= current_user.email %></a></li>
		<li><%= 
				link_to_if(current_user.profile.nil?, "Профиль", new_applicant_resume_path) do 
					link_to "Профиль", edit_applicant_profile_path(current_user.profile.id)
				end
			%>
		</li>
		
		<li>
			<%= 
				link_to_if(current_user.resume.nil?, "Резюме", new_applicant_resume_path) do 
					link_to "Резюме", applicant_resume_path(current_user.resume.id)
				end
			%>
		</li>
</ul>
