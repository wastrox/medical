<div class="cap">
	<img src="/assets/save.png" class="icon-save"><a href="#" class="save-link">Сохранить</a>
	<img src="/assets/delete.png" class="icon-vdelete"><%= link_to("Удалить", applicant_resume_path(@resume.id), :method => :delete, :confirm => 'Вы, уверены, что хотите удалить резюме') %>
	<img src="/assets/cancel.png" class="icon-cancel"><%= link_to("Скрыть", :controller => "resumes", :action => "defer", :id => @resume.id) %>
</div>
<div class="boxed">
	<% if flash[:notice] %>
	      <br>
	      <div class="alert alert-block alert-success">
	            <p id="notice"><%= flash[:notice] %></p>
	      </div>
	<% end %>
	<%= nested_form_for([:applicant, @resume], :html => { :class => 'standart_form' } ) do |applicant_fields| %>				
		<%= render 'resume', { :applicant_fields => applicant_fields } %>

		<span class="line-bottom line-bottom-company"></span>
		
		<div id="button-bottom">
			<%= link_to "Отменить", "/", :id => "btn-cancel", :class => "input-sub btn btn-danger" %>
			<%= applicant_fields.submit "Сохранить", :class => "input-sub btn btn-warning", :name => "save" %>
		</div>
	<% end %>
	<div class="clear"></div>
</div>
